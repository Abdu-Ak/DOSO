export const metadata = {
  title: "Login",
  description: "Log in to the Darul Hidaya Dars portal.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
