export const metadata = {
  title: "Forgot Password",
  description: "Reset your Darul Hidaya Dars account password.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ForgotPasswordLayout({ children }) {
  return <>{children}</>;
}
